var a:int;
var b:real;var c:boolean;
b :=b+3*a;
loop
        var p :real;
        var d : int;
        if a > b
        then
                c := 3 * p >= d / 4;
        else
                var c : real;
                b := p - c + 17 / a;
        end if;
        assert c;
        exit when d <= a+d*3 div 5 mod p;
end loop;
var a,z :int;
var b:real;
var c:boolean;
b := c + 3 * a;
assert c;

var a:record z:int; y:real end record;
var e:record z:int; y:real end record;
loop
        var a:boolean; 
        bind p to e.z;
        
        if   a > d.z then exit; 
        else
        	 bind var r to e.y;
        	 r := 2.34;
        end if;

        exit when e <= z+3 div 4 mod p;
end loop;

a := 5.67;